---
# default: --profile pretty
# pretty: --format pretty
# progress: --format progress

default: --publish-quiet -p html -p bdd -p json
html: --format html --out=test_report/report_test.html
dot: --format progress
bdd: --format pretty
json: --format json -o "report.json"